# Django environment config example

SECRET_KEY=your-secret-key
DEBUG=True
ALLOWED_HOSTS=.kavia.ai,localhost,127.0.0.1,testserver

# Database options (use for Postgres/MySQL. Will fallback to sqlite3 if not set)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=notes_db
DB_USER=notes_user
DB_PASSWORD=notes_pass
DB_HOST=localhost
DB_PORT=5432
